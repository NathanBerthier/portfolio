<div class='posts'>
  <% @posts.reverse.each do |post| %>
    <%= link_to post_path(post) do%>
      <div class='post-card'>
        <h2><%= post.title %></h2>
        <p><%= post.description %></p>
        <h3>#<%= post.id %></h3>
      </div>
    <% end %>
  <% end %>
</div>
